/*document.observe( 'dom:loaded', function() {
	$$('.ep_sr_component > a[name="medshare"]').each(function(e) {
		alert($(e).up('div').id);
	}
Event.observe($("c4_medshare_programme"), 'change', function(e){ alert("changed!"); });
});*/
